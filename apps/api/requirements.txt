# SAMVAAD API gateway — runtime dependencies
# Pinned to minor versions: reproducible enough to debug, loose enough to patch.

fastapi>=0.115,<0.116
uvicorn[standard]>=0.32,<0.33
pydantic>=2.9,<3.0
pydantic-settings>=2.6,<3.0
httpx>=0.27,<0.29

# database (M1 onwards)
sqlalchemy>=2.0,<2.1
asyncpg>=0.30,<0.31
alembic>=1.14,<1.15
